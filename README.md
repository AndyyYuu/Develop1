# 焕墟幻境·破壁

一个基于山海经世界观的亲子互动游戏，帮助家长和孩子通过"双镜交辉"的方式理解彼此的认知差异，建立更深的情感连接。

## 🎮 游戏简介

在虚空中捡到一枚「裂隙玉珏」，被传送到山海经世界边缘的「识海关」。此处是现实与内心的交界处，弥漫着「认知迷雾」。守境人烛阴子将引导你们完成一场特殊的修行之旅。

### 核心玩法

1. **双生地图**：家长和孩子分别填写问卷，背对背绘制各自的心域图
2. **点灵术**：标记微光（认同）和裂隙（好奇），发现彼此的认知差异
3. **真言法会**：分享感受，学习理解彼此的视角，建立更深的连接

### 适用人群

- **适合年龄**：8岁以上
- **参与人数**：2人（家长+孩子）
- **所需时间**：约40分钟
- **环境要求**：安静的空间，准备纸笔记录

## 🚀 快速开始

### 方式一：在线访问（推荐）

直接访问部署好的网站：[点击这里访问](https://3000-i116oqqn7n9dohokoyw1t-53bbd33f.us2.manus.computer)

### 方式二：本地运行

#### 前置要求

- Node.js 18+ 
- pnpm 包管理器

#### 安装步骤

1. **克隆项目**

```bash
git clone <repository-url>
cd pobi-game
```

2. **安装依赖**

```bash
pnpm install
```

3. **启动开发服务器**

```bash
pnpm dev
```

4. **访问应用**

打开浏览器访问：`http://localhost:3000`

#### 构建生产版本

```bash
# 构建静态文件
pnpm build

# 启动生产服务器
pnpm start
```

构建后的文件将输出到 `dist/` 目录。

### 方式三：GitHub Pages 部署

1. **Fork 本项目到你的 GitHub 账户**

2. **启用 GitHub Pages**
   - 进入项目的 Settings → Pages
   - Source 选择 "GitHub Actions"

3. **创建部署工作流**

在项目根目录创建 `.github/workflows/deploy.yml`：

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          
      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8
          
      - name: Install dependencies
        run: pnpm install
        
      - name: Build
        run: pnpm build
        
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v2
        with:
          path: ./dist/public

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v2
```

4. **推送代码触发部署**

```bash
git add .
git commit -m "Add deployment workflow"
git push origin main
```

5. **访问你的网站**

部署完成后，访问：`https://<your-username>.github.io/<repository-name>/`

## 📁 项目结构

```
pobi-game/
├── client/                 # 前端代码
│   ├── public/            # 静态资源
│   │   └── images/        # 图片资源
│   └── src/
│       ├── pages/         # 页面组件
│       │   ├── Home.tsx           # 首页
│       │   ├── GameIntro.tsx      # 游戏介绍
│       │   ├── ParentForm.tsx     # 家长问卷
│       │   ├── ChildForm.tsx      # 孩子问卷
│       │   ├── Comparison.tsx     # 对比标记
│       │   ├── Sharing.tsx        # 分享交流
│       │   └── Summary.tsx        # 总结页面
│       ├── components/    # 可复用组件
│       ├── App.tsx        # 应用入口
│       └── index.css      # 全局样式
├── server/                # 服务器代码（静态服务）
├── package.json           # 项目配置
└── README.md             # 本文件
```

## 🎨 设计风格

**新国潮赛博修仙**

- **色彩**：深邃玄黑 + 荧光青 + 朱砂红 + 紫色雷纹
- **字体**：思源宋体（标题）+ 思源黑体（正文）
- **视觉元素**：粒子动画、发光效果、卷轴纹理、八卦图案
- **世界观**：山海经 × 赛博朋克 × 修仙文化

## 🛠️ 技术栈

- **前端框架**：React 19
- **路由**：Wouter
- **样式**：Tailwind CSS 4
- **UI组件**：shadcn/ui
- **字体**：Google Fonts (Noto Serif SC, Noto Sans SC)
- **构建工具**：Vite
- **包管理器**：pnpm

## 📝 使用说明

1. **首页**：了解游戏背景和玩法
2. **游戏介绍**：烛阴子引导，介绍识海关的规则
3. **家长问卷**：家长独立完成3个问题
4. **孩子问卷**：孩子独立完成3个问题
5. **对比标记**：双方查看彼此答案，标记"微光"或"裂隙"
6. **分享交流**：按照"借光诀"心法进行深度对话
7. **总结页面**：查看修行成果，下载记录，获得今夜试炼任务

## 💡 核心理念

> "墙壁护人安全，亦隔绝了星光。今日我们不拆墙，只拍一张灵识X光——看清纹理，发现那天然的缝隙。记住，缝隙不是破损，是光与风唯一的通道。"
> 
> —— 烛阴子

这不是一个找对错的游戏，而是一次**认知图式探索**。通过可视化双方的视角差异，帮助家长和孩子理解：

- 同一件事，不同人会有不同的感受
- 分歧很正常，重要的是愿意理解对方
- 裂隙不是问题，两域永隔才是劫数

## 🔒 隐私说明

- 所有数据仅存储在浏览器本地（localStorage）
- 不会上传到任何服务器
- 建议下载修行记录后妥善保管
- 清除浏览器数据会删除所有记录

## 📄 开源协议

MIT License

## 🙏 致谢

- 设计灵感来源于山海经神话体系
- 感谢所有为亲子关系改善而努力的家庭
- 特别感谢"破壁"课程的原创者

---

**愿每个家庭都能找到属于自己的"光之通道"** ✨
